{% extends 'admin/base.html.twig' %}

{% block title %}Administration - Editions LC{% endblock %}

{% block body %}
    <center>
        {% if error == -1 %}
            <p style="width: 30%;" class="alert alert-success">Salon ajouté avec succès!</p>
        {% endif %}
        {% if error == 1 %}
            <p style="width: 30%;" class="alert alert-danger">Ce salon existe déjà!</p>
        {% endif %}
        <div style="width: 30%; border: 1px solid black; background-color: #efefef">
            <h3 style="font-weight:bold;text-decoration:underline;">Ajouter un salon</h3>
            <br/>
            {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
            {{ form_label(form.nom) }}
            {{ form_widget(form.nom) }}<br/>
            {{ form_label(form.date) }}
            {{ form_widget(form.date) }}<br/>
            {{ form_label(form.ville) }}
            {{ form_widget(form.ville) }}<br/>
            <div class="row">
                <div>
                    {{ form_widget(form.annuler) }}
                    <a style="margin-left:50px">{{ form_widget(form.valider) }}</a>
                    {{ form_end(form) }}
                </div>
            </div>
            <br/>
        </div>
    </center>
{% endblock %}
